<%= simple_nested_form_for(@ride) do |f| %>
  <% if @ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ride.errors.count, "error") %> prohibited this ride from being saved:</h2>

      <ul>
      <% @ride.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
      <%= f.label :driver_id %>
      <%= f.collection_select :driver_id, User.all, :id, :name %>
  </div>
  <div class="field">
      <%= f.label :requestor_id %>
      <%= f.collection_select :requestor_id, User.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :initial %>
    <%= f.text_field :initial %>
  </div>
  <div class="field">
    <%= f.label :destination %>
    <%= f.text_field :destination %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

